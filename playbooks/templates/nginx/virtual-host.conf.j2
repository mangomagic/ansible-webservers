# Virtual Host configuration for {{ item }}

server {
	listen 80;
	listen [::]:80;

	server_name {{ item }} www.{{ item }};

	root /var/www/{{ item }};
	index index.html index.php;

	location / {
		try_files $uri $uri/ =404;
	}
}